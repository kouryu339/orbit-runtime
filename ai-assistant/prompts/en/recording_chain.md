## Current Operation Chain (Recording)

The full current operation chain is shown below. When editing it, use this text as the base, generate the complete updated chain, and overwrite it through `RecorderWriteSystem`.

```
{{CHAIN}}
```
