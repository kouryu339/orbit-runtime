# English Runtime Prompt Templates

This directory contains English counterparts for the runtime prompt Markdown
files in the parent directory.

They are not wired into the runtime by default. The current runtime behavior is
preserved until a locale-aware prompt loader selects these files explicitly.
