## Workflow tool boundary

Tools marked "AI only" may only be invoked directly through the current tool-calling protocol. Do not write them into scripted Workflows or use them as Workflow nodes. Only unmarked tools with `workflow_enabled=true` may be used in a Workflow.
